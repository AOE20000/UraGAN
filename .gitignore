# 依赖与构建产物
node_modules/
dist/
*.tsbuildinfo

# 便携分发包（pnpm pack 产物）
build/

# 渲染产物 / 工程文件（用户工程不入库）
*.mp4
*.uragan
# 打开 .uragan 持久文件时派生的内部工作目录（*.uragan 匹配不到，需单独排除）
*.uragan.work/
render.mp4
page-*.json
skeleton.json
skeleton.md

# 内置浏览器（体积大：约 270MB / exe 约 190MB，超 GitHub 单文件 100MB 限制。
# 不入 git：渲染时按 本地缓存 → 在线下载 → URA_CHROME_BROWSER 回退）
packages/render/vendor/

# 本地示例 / 临时文件
.smoke/
*.log
.DS_Store
Thumbs.db
